# Base URL of the deployed Warden server (no trailing slash).
# Leave unset for local dev — next.config.mjs proxies API routes to
# http://localhost:4000 automatically when this is empty.
NEXT_PUBLIC_WARDEN_URL=https://your-warden-host.example.com
